# Legacy Scripts

These scripts are retained for historical reference only.

They are intentionally excluded from the standard recovery flow because they can
conflict with the current contract/demo setup or seed alternate data shapes.

Use `../README_recovery_order.md` for the canonical migration/seed/verify order.
