# Create before install:
# kubectl create secret generic gravitre-secrets \
#   --from-literal=SUPABASE_URL=... \
#   --from-literal=SUPABASE_SERVICE_ROLE_KEY=... \
#   --from-literal=CONNECTOR_SECRETS_ENCRYPTION_KEY=...
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.existingSecret }}
type: Opaque
stringData:
  PLACEHOLDER: "replace-with-kubectl-create-secret"
