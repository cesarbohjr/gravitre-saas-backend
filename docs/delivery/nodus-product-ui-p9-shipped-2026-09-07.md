# Nodus product UI — P9 shipped (2026-09-07)

**Status:** Shipped to `main` (await Vercel Ready + authenticated `/ai` visual check)  
**Gate:** Cesar “move to P9” — AI Chat normalize

## Delivered

| Layer | Change |
|-------|--------|
| Bubbles | `chat-typography.ts` — brand-green user, mineral assistant (`border-divide` / `--g-surface-1`) |
| Shell CSS | `.ai-chat-surface` / `.ai-chat-canvas` → `--g-canvas` + `--divide` |
| Composer | Shared controls — `--np-radius-lg`, brand send, no hard-coded `#16a374` / ChatGPT white pill |
| Agent Elements | plan / tool / clarify / subagent chrome → Nodus surface shells |
| Follow-ups + chips | divide + brand hover (not `rounded-full bg-card`) |
| Agent chat header | matches `/ai` surface tokens |
| Mode selector | violet/sky retired for brand/signal tokens |

## Scaffold honesty

**(a)** Explicitly authorized (“move to P9”). No new prices, claims, TRAINED badges, or Enable toggles. No ChatGPT skin retained on primary bubbles/composer.

## Evidence

- Deploy: cite Vercel Ready id after push.
- Visual PASS: **not claimed** until signed-in `/ai` screenshot vs mineral shell.
