# Nodus chrome correction — quiet labels + Learning cards (2026-09-07)

**Status:** Shipped to `main` (await Vercel Ready)  
**Gate:** Cesar — multi-color Learning cards bad; highlights only on important status; Connectors top space + minimize

## Fixes

| Issue | Change |
|-------|--------|
| Learning multi-color cards | White `--g-surface-1` cards; metrics graphite; soft pill **only** on Ready / Watch / Needs attention |
| Soft pills on every label | `Badge` default/outline quiet again; soft fills reserved for `status` / `warning` / `destructive` |
| Connectors Available / readiness | Quiet outline + status dots — not green soft fills on every tag |
| Connectors header height | **Minimize** collapses filters, stats, recommendations, available strip (persisted) |
| Top menu height | Top bar **chevron** minimizes org/env/Admin chrome (persisted) |

## Scaffold honesty

**(a)** Explicitly authorized in this conversation. No new prices/claims/Enable toggles.
