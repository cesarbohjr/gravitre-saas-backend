PHASE 3 — Extend Recipes/Dependency Graph, only if Phase 0 confirms real 
gap
═══════════════════════════════════════════════════
1. If Phase 0 confirms Meson/workflow templates do NOT already cover 
   the "capability-referenced, not vendor-referenced, reusable business 
   process" concept, scope a real, bounded extension: department 
   recipes (e.g., "new lead enrichment," "employee onboarding") defined 
   in terms of the NEW capability ontology from Phase 1, so the same 
   recipe correctly resolves differently for a Microsoft+HubSpot customer 
   versus a Google+Salesforce customer.
2. If Phase 0 confirms this already substantially exists, extend it to 
   use the new capability layer rather than building a parallel system.
═══════════════════════════════════════════════════
PHASE 4 — Verify agents communicate this gracefully, reusing the 
already-shipped conversational layer
═══════════════════════════════════════════════════
1. This does NOT require new conversational work, the shared 
   conversational-behavior layer (rules 1-10, already shipped and 
   verified) already governs HOW agents communicate. Confirm the NEW 
   capability-resolution work composes correctly with it: when an agent 
   resolves a capability to a specific vendor action, it should 
   communicate this naturally and transparently ("I'll create that 
   contact in your HubSpot," not a robotic recitation of internal 
   capability IDs), live-tested with a real, multi-turn conversation.
2. Confirm the honesty/verified-completion discipline applies 
   identically regardless of whether an action was reached via direct 
   naming or capability resolution, no degradation in trust or 
   transparency from this new abstraction layer.
═══════════════════════════════════════════════════
VERIFICATION
═══════════════════════════════════════════════════
Real, complete Phase 0 reconciliation. Real, live proof the capability 
ontology correctly resolves to the right vendor action, respects 
existing narrowing and write-authority, and handles multi-tool 
ambiguity correctly. Real, scoped Phase 2 connector list with explicit 
backlog, not an unbounded build. Real Phase 3 finding on Recipes/ 
Dependency Graph overlap. Real, live conversational test confirming 
graceful, natural communication of capability-resolved actions. Zero 
regression to any existing catalog, governance, or conversational work.
COMMIT, PUSH, MERGE, DEPLOY, confirm live git_sha, run all verification 
against the deployed tip.
</user_query>