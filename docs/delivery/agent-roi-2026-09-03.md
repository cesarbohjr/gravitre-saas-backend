# Agent ROI — live delivery (2026-09-03)

## Scope

Phase 1 compute + Phase 2 UI for real, per-agent ROI from measured COGS and operational counts, with Module C honesty labels on estimates.

## Surfaces

| Surface | Path |
|---------|------|
| API | `GET /api/enterprise/agent-roi?period_days=30` |
| Org + per-agent UI | `/settings/enterprise?tab=roi` |
| Intelligence Reports ROI tab | `/intelligence/reports` (ROI tab → `AgentRoiPanel`) |

## Honesty

- **Measured:** `agentCostUsd` = `SUM(model_calls.cost_usd)`
- **Operational:** tasks completed, actions executed
- **Estimate:** hours saved, labor value, ROI multiple
- **not_configured:** revenue influenced unless outcome metadata has a real monetary amount
- Default labor rate `$45/hr` only when org settings unset — labeled `default_estimate`, not a billed SKU

## Product-surface declaration

**(a)** Explicitly requested in this conversation (Phase 1–2 agent ROI). Labor default is an estimate input, not an invented customer price or Enable toggle.

## Evidence

Live probe `scripts/verify-agent-roi-live.py` → `docs/delivery/agent-roi-live.json`:

- **PASS** — agent ROI honesty + measured cost match
- `api_git_sha`: `a78ef32df8639449b52dd259adf6a24653bbd179`
- `checked_at`: see JSON
- Cost cross-check: API `orgTotals.agentCostUsd` matches Supabase `model_calls` sum for the period window

Unit: `pytest backend/tests/services/test_agent_roi_service.py` — 6 passed (with reporting honesty tests: 12 passed).

## Deploy

Pushed to `main` as `a78ef32d`. Prod `/health` confirmed `git_sha=a78ef32df8639449b52dd259adf6a24653bbd179`.
