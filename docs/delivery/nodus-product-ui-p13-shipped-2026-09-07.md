# Nodus product UI — P13 shipped (2026-09-07)

**Status:** Shipped to `main` (await Vercel Ready; extension load-unpacked for overlay check)  
**Gate:** Cesar “move to P13 Extension” — Compressed same system: Context / Capture / Agent / Action / Approval / Status

## Delivered

| Layer | Change |
|-------|--------|
| Tokens | `popup.css` + `overlay.css` — `--g-*` / `--np-*` / `--divide` aliases |
| Overlay | Six labeled sections in `shared.js` (same enrich/approve/chat behavior) |
| Popup / sidepanel | Status + Action (and Context on sidepanel) compress chrome |
| `/extension/connect` | Mineral surface shell (auth bridge only) |

## Scaffold honesty

**(a)** Explicitly authorized (“move to P13 Extension”).  
No new prices, claims, badges, or Enable entitlement toggles. Behavior unchanged — chrome + section compress only.

## Evidence

- Deploy: cite Vercel Ready id after push (web connect page).
- Extension overlay PASS: **not claimed** until load-unpacked visual check on a host page.
