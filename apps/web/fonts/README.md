# Gravitre fonts (Nodus full-match)

| Family | Role | Origin |
|--------|------|--------|
| **Inter Display** | Primary UI / marketing (`--font-sans`) | Licensed Nodus Agent Template (Aceternity); TTFs under `inter-display/` |
| **DM Mono** | Mono / labels (`--font-mono`) | Google Fonts via `next/font` |

`vendor/nodus-agent-template/` stays **gitignored**. These files are the committed, license-backed mirror required for design match across web / desktop / extension.

Do not invent alternate brand fonts without Cesar approval.
