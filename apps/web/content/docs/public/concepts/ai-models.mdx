---
title: AI Models
description: Org-scoped model registry for training, validation, deployment, and inference (Command plan).
audience: public
depth: high
tier: command
status: published
category: Core Concepts
readTime: 5 min
prev:
  title: Training Hub
  href: /docs/concepts/training
next:
  title: Assistant
  href: /docs/concepts/assistant
---

**AI Models** (`/models`) is the control plane for production machine learning in your organization — separate from the lighter **Training Hub** fine-tune flow.

Requires **Command** plan or above for API access.

## Model layers

The registry organizes work into layers:

| Layer | Typical use |
|-------|-------------|
| **Generative & agents** | Fine-tuned LLMs, agent routing |
| **Tabular & classification** | Lead scoring, churn, routing |
| **Forecasting** | Demand and revenue projections |
| **Anomaly detection** | Ops and billing outliers |
| **MLOps** | Deployment pipelines tied to workflows |

Templates suggest model type, task, and base model for each layer.

## Lifecycle

```text
Draft → Training / validating → Ready → Deployed → (archived)
```

Deployed models expose **predict** endpoints for workflows and services. Training may pull from connected **Sources** when configured.

## AI Models vs Training Hub

| | **Training Hub** | **AI Models** |
|---|------------------|---------------|
| **Focus** | Agent fine-tunes, instructions, quick jobs | Full registry, deploy, inference |
| **Audience** | Builders improving agents | ML ops / advanced teams |
| **Plan** | Standard paid tiers | **Command+** |

Many teams start in Training Hub and promote stable models into AI Models for production deployment.

## Next steps

- [Manage AI Models](/docs/guides/how-to/ai-models)
- [Connect Sources](/docs/guides/how-to/sources) for training data
