"use client"

/**
 * QUARANTINED — template compliance badge strip (unverified CCPA/GDPR/ISO art).
 * Use `/security` for authorized Gravitre security copy.
 */
export function Security() {
  return null
}
