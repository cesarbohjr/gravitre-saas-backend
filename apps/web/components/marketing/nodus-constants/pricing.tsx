/**
 * QUARANTINED (Phase 10 residue audit).
 * Template demo prices must never render on Gravitre marketing.
 * Authorized plans live in `@/lib/pricing-page-data` + `/pricing`.
 */
export enum TierName {
  TIER_1 = "Growth",
  TIER_2 = "Scale",
  TIER_3 = "Enterprise",
}

export const tiers: never[] = []
export const pricingTable: never[] = []
