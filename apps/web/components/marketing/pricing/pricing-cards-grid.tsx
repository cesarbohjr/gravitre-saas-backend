"use client"

/**
 * Legacy card grid — superseded by Nodus `Pricing` on `/pricing`.
 * Kept as a thin re-export so older imports keep resolving.
 */
export { Pricing as PricingCardsGrid } from "@/components/marketing/nodus/pricing"
