/**
 * Lite uses the same AppShell as Admin (via LitePageShell on each page).
 * A separate chrome fork made Lite feel like a different product.
 */
export default function LiteLayout({ children }: { children: React.ReactNode }) {
  return children
}
