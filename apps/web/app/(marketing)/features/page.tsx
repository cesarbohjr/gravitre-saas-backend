import { FeaturesPage } from "@/components/marketing/features/features-page"

/**
 * Features — Nodus full-match composition (same chrome / animations / hero image
 * as the homepage). Authorized product copy only; no legacy muted bento layout.
 */
export default function FeaturesRoute() {
  return <FeaturesPage />
}
