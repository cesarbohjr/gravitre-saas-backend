{
  "name": "gravitre-operator-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && python -m uvicorn app.main:app --reload --port 8000",
    "dev:web": "cd apps/web && pnpm dev",
    "dev:full": "node scripts/dev.mjs",
    "dev": "cd apps/web && pnpm dev"
  }
}
